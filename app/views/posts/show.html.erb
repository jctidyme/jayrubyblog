<h1><%= @post.title %></h1>
<p><%= @post.body %></p>
<hr>
<p><small><%= @post.created_at.strftime("%b %d. %y") %></small></p>

<%= link_to "Edit Post", edit_post_path %> | <%= link_to "Delete", @post, data: { confirm: 'Are you sure you want to delete this alert?' }, :method => :delete %>

